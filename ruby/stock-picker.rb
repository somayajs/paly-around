# a #stock_picker take an array
# lowerest = array[0]
# highest = array[-1]
# difference = highest - lowerest
# loop through the array to find the lowerest number exclusive the last element
# if highest - array[i] > difference and highest - array[i] > 0
